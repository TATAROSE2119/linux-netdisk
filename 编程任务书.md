# NetDisk eBPF Debugger BCC 到 libbpf/CO-RE 迁移编程任务书

## 1. 任务概述

### 1.1 项目名称

NetDisk eBPF Debugger 的 BCC 到 libbpf/CO-RE 渐进式迁移。

### 1.2 任务目标

在不改变 Flask 面板和 `collectors` 对外数据结构的前提下，将 `ebpf_debugger/bpf_programs/` 中依赖 BCC 的 Python 内嵌 eBPF 实现，逐步替换为：

- 内核态：C 语言 libbpf/CO-RE eBPF 程序；
- 用户态：C 语言 libbpf loader；
- 进程间接口：loader 通过 stdout 每行输出一个 JSON 事件；
- Python 适配层：保留现有类名和方法，把 JSON 事件转换为 collector 回调。

### 1.3 第一里程碑（MVP）

第一里程碑只迁移 NetworkMonitor 的 TCP connect、accept、close，必须同时满足：

1. `ebpf_debugger/libbpf` 中执行 `make` 可成功构建；
2. `sudo ./netdisk_loader --network` 可持续输出合法 JSON 行；
3. `NetworkMonitor` 可启动 loader 并调用原有 `NetworkCollector`；
4. Web 面板和 `/api/network` 可看到连接数据变化。

在 MVP 通过前，不得开始 syscall、perf 和 uprobe 迁移。

## 2. 当前基线

根据 2026-07-16 的仓库和本机检查结果：

- 当前内核为 `6.6.87.2-microsoft-standard-WSL2+`，架构为 `x86_64`；
- `/sys/kernel/btf/vmlinux` 存在，具备 CO-RE 基础条件；
- `clang`、`bpftool`、`gcc`、`make`、`pkg-config` 可用；
- `pkg-config` 尚未识别出 libbpf 版本，开始编码前需确认/安装 `libbpf-dev`；
- `ebpf_debugger/libbpf/Makefile` 已存在，但是空文件；
- 四个 `bpf_programs/*.py` 仍使用 `from bcc import BPF`；
- `app.py` 通过 `start()`、`poll()`、`stop()` 统一驱动四个 monitor。

## 3. 范围与约束

### 3.1 本任务包含

- network connect/accept/close 事件；
- network send/recv 实际成功字节数；
- read、write、openat、close、accept4、sendto、recvfrom syscall 耗时；
- sched_switch 和后续 CPU 采样；
- `handle_client`、`send_directory_tree`、`sha256_string` uprobe/uretprobe；
- Python 适配、启动脚本、文档和验收用例。

### 3.2 本任务不包含

- 重写 Flask 页面或 collector；
- 在第一阶段追踪 IPv6；
- 在 syscall 第一版中追踪文件名；
- 在 MVP 中追求 CPU 使用率的严格百分比；
- 同时全量重写四个监控模块。

### 3.3 强制约束

1. `app.py` 对 monitor 的调用方式保持不变。
2. 以下 Python 类名与核心方法必须保留：
   - `NetworkMonitor(collector, pid_filter=None)`；
   - `SyscallTracer(collector, pid_filter=None, comm_filter=None)`；
   - `PerfAnalyzer(collector, target_pids=None)`；
   - `UprobeTracer(collector, server_path=None, client_path=None)`；
   - 每个类保留 `start()`、`poll(timeout=...)`、`stop()`。
3. `collectors/*.py` 在迁移期间原则上不改。
4. loader 的 stdout 只输出 JSON 事件；日志和错误必须输出到 stderr。
5. 每个 JSON 事件占一行，输出后立即 `fflush(stdout)`。
6. BPF 和 loader 必须共用同一份 `include/events.h`，事件中不得放指针。
7. send/recv 字节数必须使用 kretprobe 返回值 `ret > 0`，不得使用入口的请求长度充当实际字节数。
8. Python 不得再内嵌 eBPF C 字符串，也不得依赖 BCC。

## 4. 目标架构与接口

### 4.1 目标目录

```text
ebpf_debugger/
├── app.py
├── collectors/                  # 保持现有聚合逻辑
├── bpf_programs/                # Python 适配层
│   ├── network_monitor.py
│   ├── syscall_tracer.py
│   ├── perf_analyzer.py
│   └── uprobe_tracer.py
└── libbpf/
    ├── Makefile
    ├── vmlinux.h               # 生成文件
    ├── include/
    │   └── events.h
    ├── netdisk_bpf.bpf.c
    ├── netdisk_loader.c
    ├── netdisk_bpf.bpf.o       # 生成文件
    ├── netdisk_bpf.skel.h      # 生成文件
    └── netdisk_loader           # 生成文件
```

### 4.2 数据流

```text
kernel probe
    -> BPF ring buffer
    -> netdisk_loader
    -> stdout JSON Lines
    -> bpf_programs/*.py
    -> collectors/*.py
    -> Flask API / WebSocket
    -> dashboard
```

### 4.3 Network JSON 合同

```json
{"type":"network","action":1,"pid":1234,"tid":1234,"comm":"curl","saddr":"127.0.0.1","daddr":"127.0.0.1","sport":51000,"dport":8080,"bytes":0,"timestamp_ns":123456789}
```

`action` 的固定映射：

| action | 含义 | collector 回调 |
|---:|---|---|
| 1 | connect | `on_connect(...)` |
| 2 | accept | `on_connect(...)` |
| 3 | close | `on_close(...)` |
| 4 | send | `on_send(..., bytes)` |
| 5 | recv | `on_recv(..., bytes)` |

### 4.4 后续模块 JSON 合同

Syscall：

```json
{"type":"syscall","pid":1234,"tid":1234,"comm":"server","syscall":"read","duration_ns":4500,"ret":128,"timestamp_ns":123456789}
```

Perf 上下文切换：

```json
{"type":"perf","event":"context_switch","prev_pid":10,"prev_comm":"a","next_pid":20,"next_comm":"b","timestamp_ns":123456789}
```

Perf CPU 采样：

```json
{"type":"perf","event":"cpu_sample","pid":1234,"comm":"server","cpu":2,"timestamp_ns":123456789}
```

Uprobe：

```json
{"type":"uprobe","event":"return","pid":1234,"tid":1234,"comm":"server","func":"handle_client","timestamp_ns":123456789,"duration_ns":8000,"ret":0}
```

JSON 字符串字段必须正确转义；`comm` 等固定长度 C 字符数组在输出前必须保证结尾安全。

## 5. 分阶段编程任务

## 任务 0：环境与基线确认

### 操作

- [ ] 确认 BTF：`test -r /sys/kernel/btf/vmlinux`。
- [ ] 确认工具：`clang --version`、`bpftool version`、`gcc --version`。
- [ ] 确认 libbpf：`pkg-config --modversion libbpf`。
- [ ] 如 libbpf 缺失，安装 `libbpf-dev`、`libelf-dev`、`zlib1g-dev`。
- [ ] 记录 `uname -r`、`uname -m` 和 WSL2 环境。
- [ ] 先运行一次现有项目，记录 `/status`、`/api/network`、`/api/syscall`、`/api/perf` 返回结构。

### 完成标准

`pkg-config --exists libbpf` 返回 0，BTF 文件可读，且已保存迁移前 API 基线。

---

## 任务 1：建立 libbpf 构建骨架

### 修改文件

- `ebpf_debugger/libbpf/Makefile`
- `ebpf_debugger/libbpf/include/events.h`
- `ebpf_debugger/libbpf/netdisk_bpf.bpf.c`
- `ebpf_debugger/libbpf/netdisk_loader.c`
- `.gitignore`

### 操作

- [ ] Makefile 增加 `vmlinux.h -> .bpf.o -> .skel.h -> loader` 依赖链。
- [ ] 根据 `uname -m` 生成 `__TARGET_ARCH_x86` 或 `__TARGET_ARCH_arm64`。
- [ ] BPF 编译参数至少包含 `-g -O2 -target bpf`。
- [ ] loader 链接 `libbpf`、`libelf`、`zlib`。
- [ ] `events.h` 先定义统一事件类型、network action 和 network payload。
- [ ] `netdisk_bpf.bpf.c` 加入 license 和 ring buffer map。
- [ ] loader 完成 skeleton open/load/attach、ring buffer poll、SIGINT/SIGTERM 退出和资源释放。
- [ ] `.gitignore` 忽略 `vmlinux.h`、`*.bpf.o`、`*.skel.h`、`netdisk_loader`。

### 实现要点

- 保留 `V=1` 或等价方式，便于查看完整编译命令。
- `clean` 只删除生成物，不删除源文件。
- loader 返回非 0 时必须在 stderr 说明失败阶段。
- 调试期启用 libbpf 日志回调，不要把日志写到 stdout。

### 验收

```bash
cd ebpf_debugger/libbpf
make clean
make
test -x netdisk_loader
test -s netdisk_bpf.bpf.o
test -s netdisk_bpf.skel.h
```

### 完成标准

全新环境只要具备 BTF 和依赖，一条 `make` 命令即可生成三个目标产物。

---

## 任务 2：实现 Network MVP 的内核态程序

### 修改文件

- `ebpf_debugger/libbpf/include/events.h`
- `ebpf_debugger/libbpf/netdisk_bpf.bpf.c`

### 操作

- [ ] 实现 `read_sock_info()`，使用 `BPF_CORE_READ` 读 IPv4 地址和端口。
- [ ] 在 `tcp_v4_connect` kprobe 保存 `pid_tgid -> {timestamp, sock}`。
- [ ] 在 `tcp_v4_connect` kretprobe 中仅接受 `ret == 0` 或 `ret == -EINPROGRESS`。
- [ ] 在 `inet_csk_accept` kretprobe 中输出 accept 事件。
- [ ] 在 `tcp_close` kprobe 中输出 close 事件。
- [ ] 增加 `target_pid` 只读配置；0 表示不过滤。
- [ ] 所有临时 map 在成功、失败和 ringbuf reserve 失败路径上都清理。

### 实现要点

- ring buffer 事件 reserve 后先完整清零，再逐字段填充。
- reserve 成功后的每条分支都必须 submit 或 discard。
- `skc_dport` 使用 `bpf_ntohs`；IPv4 地址字节序在 loader 中统一处理。
- `pid` 取 `pid_tgid >> 32`，`tid` 取 `(__u32)pid_tgid`。
- BPF 程序不做 JSON 拼接。

### 验收

```bash
cd ebpf_debugger/libbpf
make clean && make
sudo ./netdisk_loader --network
```

在另一终端制造事件：

```bash
curl http://127.0.0.1:8080/api/health
```

### 完成标准

loader 至少输出 connect/accept/close 中实际发生的事件，PID、进程名、IPv4 和端口显示正确，verifier 无报错。

---

## 任务 3：完成 loader 的 Network JSON 输出

### 修改文件

- `ebpf_debugger/libbpf/netdisk_loader.c`

### 操作

- [ ] 解析 `--network` 和可选 `--pid <PID>`。
- [ ] 未知参数输出 usage 并返回非 0。
- [ ] 在 load 前设置 BPF 程序的自动加载选项和 `target_pid` rodata。
- [ ] 检查 ring buffer 回调中的 `data_sz` 和事件 kind。
- [ ] 用 `inet_ntop` 转换 IPv4，按第 4.3 节输出 JSON。
- [ ] 对 JSON 字符串字段做最小完整转义。
- [ ] 每条事件立即 flush。
- [ ] 收到 SIGINT/SIGTERM 后退出 poll 并释放 link、ring buffer 和 skeleton。

### 实现要点

当一个 skeleton 中后续加入 syscall、perf、uprobe 程序时，`--network` 不应 attach 其他模块。必须在 `open` 与 `load` 之间对未选中程序调用 `bpf_program__set_autoload(prog, false)`，否则“多模式 loader”只是命令行表面分模式。

### 验收

```bash
sudo ./netdisk_loader --network | while IFS= read -r line; do
    printf '%s\n' "$line" | python3 -m json.tool >/dev/null || exit 1
done
```

### 完成标准

stdout 中不出现日志或半行 JSON，每条事件均可被标准 JSON 解析器解析，Ctrl+C 可正常退出。

---

## 任务 4：将 NetworkMonitor 改为 Python 适配层

### 修改文件

- `ebpf_debugger/bpf_programs/network_monitor.py`
- 可选新增 `ebpf_debugger/tests/test_network_monitor.py`

### 操作

- [ ] 删除 `from bcc import BPF`、内嵌 `BPF_PROGRAM` 和 BCC 事件回调。
- [ ] 使用 `__file__` 计算 loader 绝对路径，不依赖当前工作目录。
- [ ] `start()` 检查 loader 存在且可执行，然后使用 `subprocess.Popen` 启动。
- [ ] `pid_filter` 存在时传递 `--pid`。
- [ ] stdout 由 daemon thread 逐行读取并解析 JSON。
- [ ] stderr 可直接继承父进程，或由独立线程持续读取；不允许创建无人消费的 stderr pipe。
- [ ] 单行 JSON 解析失败时记录警告并继续，不得杀死读取线程。
- [ ] 严格按 action 调用 `NetworkCollector`对应方法。
- [ ] `poll()` 保留为兼容 no-op，但可检查 loader 是否意外退出。
- [ ] `stop()` 先 terminate，超时后 kill，最后 wait 和 join，且可重复调用。

### 无 root 单元测试

使用临时假 loader 输出 connect/send/recv/close JSON 和一行错误 JSON，验证：

- 对应 collector 方法的调用次数和参数；
- 错误行不影响下一行；
- `stop()` 可回收子进程；
- 未知 type/action 被安全忽略。

### 集成验收

```bash
sudo python3 ebpf_debugger/app.py
curl http://127.0.0.1:8090/status
curl http://127.0.0.1:8090/api/network
```

### MVP 完成标准

发生 TCP 连接后，`/api/network` 的 `total_connections`、`active_connections` 和 `recent_events` 发生合理变化，且无 BCC Python 依赖。

---

## 任务 5：增加 send/recv 实际字节统计

### 操作

- [ ] 定义 `io_info { sock *, requested }` 和 send/recv 临时 map。
- [ ] `tcp_sendmsg` 和 `tcp_recvmsg` 入口保存 socket 和请求长度。
- [ ] 两个 kretprobe 仅在 `ret > 0` 时发送事件，`bytes = ret`。
- [ ] 所有返回路径清理临时 map。
- [ ] Python action 4/5 分别调用 `on_send`/`on_recv`。

### 验收

使用可知长度的文件上传和下载，比较 loader JSON 字节累计值与 collector 的 `total_bytes_sent`/`total_bytes_recv`。允许协议头、分包和重试造成与文件大小不同，但不允许出现负字节或请求长度被误认为成功长度。

### 完成标准

send/recv 事件的 `bytes` 全部来自内核函数返回值，Web 面板的总发送/接收字节持续增长。

---

## 任务 6：迁移 SyscallTracer

### 操作

- [ ] 在 `events.h` 增加 syscall payload。
- [ ] 实现 read、write、openat、close、accept4、sendto、recvfrom entry/exit tracepoint。
- [ ] entry 以 `pid_tgid` 为 key 保存时间戳，exit 计算 `duration_ns` 并清理。
- [ ] PID 过滤在 entry 就执行，避免非目标进程污染 map。
- [ ] loader 增加 `--syscall`，仅加载/附加 syscall 程序。
- [ ] `syscall_tracer.py` 改为 JSON 适配层，保留 `comm_filter` 用户态过滤。

### 验收

```bash
sudo ./netdisk_loader --syscall --pid "$(pidof server)"
curl http://127.0.0.1:8090/api/syscall
```

### 完成标准

每种目标 syscall 都能产生名称、耗时和返回值；非目标 PID 不产生事件；collector 的 count、average time 和 errors 正常更新。

---

## 任务 7：迁移 PerfAnalyzer

### 7A：sched_switch

- [ ] 在 `events.h` 增加 context switch payload。
- [ ] 实现 `tracepoint/sched/sched_switch`。
- [ ] loader 增加 `--perf`并输出 `event=context_switch` JSON。
- [ ] Python 保留 `target_pids` 过滤，调用 `on_context_switch`。

### 7B：CPU 采样

- [ ] 用 `perf_event_open` 为每个 online CPU 创建 software CPU clock event。
- [ ] 用 `bpf_program__attach_perf_event` 附加 BPF 程序。
- [ ] loader 保存和销毁所有 perf links/file descriptors。
- [ ] Python 调用 `on_cpu_sample`；采样指标先明确命名为“采样权重”，不宣称为严格 CPU 百分比。

### 完成标准

`/api/perf` 的 `total_context_switches`、`context_switch_history` 和 CPU 相关数据可持续更新；关闭 loader 后不遗留 perf fd。

---

## 任务 8：迁移 UprobeTracer

### 操作

- [ ] 先构建 `server/server`，用 `nm -an` 确认三个函数符号存在。
- [ ] 定义稳定的 `func_id -> func_name` 映射。
- [ ] 临时 map key 使用 `{pid_tgid, func_id}`，避免嵌套函数覆盖外层计时。
- [ ] loader 增加 `--uprobe --server-path <absolute-path>`。
- [ ] 在用户态解析 ELF 符号偏移，显式调用 `bpf_program__attach_uprobe`。
- [ ] 入口和返回 probe 的 link 都必须保存并在 cleanup 销毁。
- [ ] `uprobe_tracer.py` 调用 `on_function_entry` 和 `on_function_return`。
- [ ] 单个符号 attach 失败时给出具体 stderr；明确决定是继续其他符号还是整体失败。

### 验收

```bash
nm -an server/server | grep -E ' handle_client$| send_directory_tree$| sha256_string$'
sudo ./netdisk_loader --uprobe --server-path "$(realpath server/server)"
```

### 完成标准

调用目标函数时可看到 entry/return 事件，duration 为正值，嵌套函数的计时不互相覆盖。

---

## 任务 9：启动脚本、配置和文档收尾

### 操作

- [ ] `start_debugger.sh` 删除 BCC 检查/安装逻辑。
- [ ] 改为检查 clang、bpftool、libbpf、BTF 和 loader 可执行性。
- [ ] 提供 `--build` 或启动前明确构建流程。
- [ ] `requirements.txt` 移除 BCC Python 依赖（如存在）。
- [ ] README 技术栈更新为 libbpf/CO-RE，补充 WSL2、BTF、root/capability 说明。
- [ ] 全仓搜索并清理过时 BCC 文案和导入。

### 验收

```bash
rg -n 'from bcc|BPF_PROGRAM|python3-bpfcc|bpfcc-tools' ebpf_debugger README.md
sudo ./ebpf_debugger/start_debugger.sh --check
```

### 完成标准

用户按 README 在新 Linux/WSL2 环境中可完成依赖检查、构建、启动和验证，运行时不需要 BCC。

## 6. 测试与验收矩阵

| 层级 | 测试内容 | 通过条件 |
|---|---|---|
| 静态构建 | `make clean && make` | 无 warning-as-error，三个生成物存在 |
| BPF 加载 | 以 root 启动各模式 | 无 verifier/CO-RE relocation 失败 |
| JSON 合同 | 逐行用 `json.loads` 解析 | 无非 JSON stdout，必填字段完整 |
| Python 单测 | 假 loader + 假 collector | 分发、容错、停止流程通过 |
| Network 集成 | connect/accept/close/send/recv | collector 连接和字节统计更新 |
| Syscall 集成 | 文件和 socket 操作 | count、duration、error 更新 |
| Perf 集成 | 负载下运行 | switch/sample 持续产生 |
| Uprobe 集成 | 调用三个 server 函数 | entry/return 成对，嵌套计时正确 |
| Web 端到端 | `/status`、`/api/*`、WebSocket | 面板数据持续变化 |
| 资源清理 | 反复 start/stop 10 次 | 无子进程、BPF link 或 fd 泄漏 |

## 7. 每阶段通用完成定义（Definition of Done）

一个任务只有同时满足以下条件才可标记完成：

- [ ] 代码可构建，无新增明显编译警告。
- [ ] 本任务的验收命令已实际执行。
- [ ] 正常路径和至少一个失败路径已测试。
- [ ] stdout JSON 合同没有被破坏。
- [ ] `start()`/`stop()` 可反复调用，无明显资源泄漏。
- [ ] 新行为已写入 README 或对应文档。
- [ ] 未提前引入下一阶段的大范围修改。

## 8. 建议提交顺序

1. `build: add libbpf build scaffold`
2. `ebpf: add network connect accept close probes`
3. `loader: emit network events as json lines`
4. `debugger: adapt network monitor to libbpf loader`
5. `ebpf: account actual tcp send and recv bytes`
6. `ebpf: migrate syscall tracepoints to core`
7. `ebpf: migrate sched and cpu sampling`
8. `ebpf: migrate server uprobes`
9. `docs: replace bcc setup with libbpf core workflow`

每次提交都应可独立构建；前个提交完成后应形成可独立演示的 Network MVP。

## 9. 推荐执行节奏

| 里程碑 | 包含任务 | 参考工作量 | 出口条件 |
|---|---|---:|---|
| M0 准备 | 任务 0 | 0.5 天 | 环境检查通过 |
| M1 构建链 | 任务 1 | 0.5–1 天 | 一键构建 |
| M2 Network MVP | 任务 2–4 | 2–4 天 | Web 面板有连接事件 |
| M3 Network 完整化 | 任务 5 | 1–2 天 | 实际 send/recv 字节数正确 |
| M4 Syscall | 任务 6 | 1–2 天 | syscall API 有数据 |
| M5 Perf | 任务 7 | 2–3 天 | switch/sample 通过 |
| M6 Uprobe | 任务 8 | 2–3 天 | 函数计时通过 |
| M7 收尾 | 任务 9 | 1 天 | 新环境按文档可运行 |

工作量仅用于排序，不作为跳过验收的理由。实际编程时应先完成 M2，再决定是否继续全量迁移。

