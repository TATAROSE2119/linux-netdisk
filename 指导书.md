# BCC 到 libbpf 迁移指导书

本文档用于指导将本项目 `ebpf_debugger` 中现有的 BCC Python 实现迁移为 libbpf/CO-RE 实现。目标不是一次性重写整个调试器，而是按模块逐步替换，保持 Flask 面板和 collector 数据结构尽量不变。

## 1. 迁移目标

当前实现位于：

```text
ebpf_debugger/
├── app.py
├── collectors/
└── bpf_programs/
    ├── network_monitor.py
    ├── syscall_tracer.py
    ├── perf_analyzer.py
    └── uprobe_tracer.py
```

这些 Python 文件使用 BCC：

```python
from bcc import BPF
```

并在 Python 字符串中内嵌 eBPF C 程序。迁移后建议改为：

```text
ebpf_debugger/
├── app.py
├── collectors/
├── bpf_programs/              # Python 适配层，保留现有类名
│   ├── network_monitor.py
│   ├── syscall_tracer.py
│   ├── perf_analyzer.py
│   └── uprobe_tracer.py
└── libbpf/                    # 新增，真正的 libbpf 实现
    ├── Makefile
    ├── include/
    │   └── events.h
    ├── netdisk_bpf.bpf.c
    └── netdisk_loader.c
```

迁移后的职责划分：

| 层级 | 职责 |
|------|------|
| `netdisk_bpf.bpf.c` | 运行在内核态的 eBPF 程序，负责采集事件 |
| `netdisk_loader.c` | 用户态 libbpf loader，加载 BPF、attach 探针、读取 ring buffer |
| `bpf_programs/*.py` | Python 适配层，启动 loader、读取 JSON 行、调用 collector |
| `collectors/*.py` | 保持现状，继续聚合数据 |
| `app.py` | 保持现状，继续启动 monitor、推送 WebSocket 数据 |

## 2. 推荐迁移顺序

不要一开始就同时迁移 network、syscall、perf、uprobe。建议按以下顺序：

1. 新建 `ebpf_debugger/libbpf/` 目录和 Makefile。
2. 先迁移 `NetworkMonitor`，只做 TCP connect/accept/close。
3. 确认 ring buffer 事件能被 loader 打印为 JSON。
4. 修改 `network_monitor.py`，让它启动 loader 并消费 JSON。
5. 再增加 send/recv 字节统计，并修正 BCC 版本中“入口参数不等于实际字节数”的问题。
6. 再迁移 syscall tracepoint。
7. 再迁移 perf event 和 sched tracepoint。
8. 最后迁移 uprobe，因为它还涉及二进制符号、可执行文件路径和函数嵌套计时问题。

这样每一步都有可验证结果，不容易陷入“代码都写了但不知道哪一层坏了”的状态。

## 3. 环境依赖

libbpf/CO-RE 版本建议在 Linux 环境下开发。WSL2 可以尝试，但需要确认内核、BTF 和权限支持。

Ubuntu/Debian 依赖：

```bash
sudo apt update
sudo apt install -y \
  clang \
  llvm \
  make \
  gcc \
  bpftool \
  libbpf-dev \
  linux-headers-$(uname -r)
```

检查 BTF：

```bash
ls -l /sys/kernel/btf/vmlinux
```

如果该文件存在，可以生成 `vmlinux.h`：

```bash
bpftool btf dump file /sys/kernel/btf/vmlinux format c > ebpf_debugger/libbpf/vmlinux.h
```

加载 eBPF 通常需要 root 或相关 capability：

```bash
sudo ./ebpf_debugger/libbpf/netdisk_loader
```

## 4. 新目录设计

建议新增：

```text
ebpf_debugger/libbpf/
├── Makefile
├── vmlinux.h                  # 由 bpftool 生成，不建议手写
├── include/
│   └── events.h               # BPF 和 loader 共享事件结构
├── netdisk_bpf.bpf.c          # BPF 程序
└── netdisk_loader.c           # libbpf 用户态 loader
```

`.gitignore` 建议加入：

```gitignore
# libbpf generated artifacts
ebpf_debugger/libbpf/*.bpf.o
ebpf_debugger/libbpf/*.skel.h
ebpf_debugger/libbpf/netdisk_loader
ebpf_debugger/libbpf/vmlinux.h
```

是否忽略 `vmlinux.h` 取决于你的策略：

| 策略 | 优点 | 缺点 |
|------|------|------|
| 提交 `vmlinux.h` | 新环境更容易构建 | 文件很大，和开发机内核绑定 |
| 不提交 `vmlinux.h` | 仓库干净 | 每台机器都要生成一次 |

教学/课程项目通常可以提交生成脚本而不提交 `vmlinux.h`。

## 5. 事件结构设计

建议先定义统一事件头，再定义不同事件 payload。

示例 `include/events.h`：

```c
#ifndef NETDISK_EVENTS_H
#define NETDISK_EVENTS_H

#include <linux/types.h>

enum netdisk_event_kind {
    NETDISK_EVENT_NETWORK = 1,
    NETDISK_EVENT_SYSCALL = 2,
    NETDISK_EVENT_PERF = 3,
    NETDISK_EVENT_UPROBE = 4,
};

enum netdisk_network_action {
    NETDISK_NET_CONNECT = 1,
    NETDISK_NET_ACCEPT = 2,
    NETDISK_NET_CLOSE = 3,
    NETDISK_NET_SEND = 4,
    NETDISK_NET_RECV = 5,
};

struct netdisk_network_event {
    __u32 pid;
    __u32 tid;
    char comm[16];
    __u8 action;
    __u32 saddr;
    __u32 daddr;
    __u16 sport;
    __u16 dport;
    __u64 bytes;
    __u64 timestamp_ns;
};

struct netdisk_event {
    __u32 kind;
    union {
        struct netdisk_network_event network;
    };
};

#endif
```

注意：

- BPF 程序和用户态 loader 必须使用同一份事件结构。
- 尽量使用固定宽度类型：`__u32`、`__u64`、`__s64`。
- 不要在事件结构里放指针。
- 字符串字段固定长度，例如 `char comm[16]`。

## 6. BPF 程序基本框架

`netdisk_bpf.bpf.c` 开头通常是：

```c
#include "vmlinux.h"
#include <bpf/bpf_helpers.h>
#include <bpf/bpf_tracing.h>
#include <bpf/bpf_core_read.h>
#include "include/events.h"

char LICENSE[] SEC("license") = "Dual BSD/GPL";
```

ring buffer map：

```c
struct {
    __uint(type, BPF_MAP_TYPE_RINGBUF);
    __uint(max_entries, 1 << 24);
} events SEC(".maps");
```

connect 临时状态 map：

```c
struct connect_info {
    __u64 ts;
    struct sock *sk;
};

struct {
    __uint(type, BPF_MAP_TYPE_HASH);
    __uint(max_entries, 10240);
    __type(key, __u64);
    __type(value, struct connect_info);
} connect_info_map SEC(".maps");
```

## 7. BCC 写法到 libbpf 写法对照

BCC map：

```c
BPF_HASH(connect_info, u64, struct connect_info_t);
connect_info.update(&pid_tgid, &info);
connect_info.lookup(&pid_tgid);
connect_info.delete(&pid_tgid);
```

libbpf map：

```c
bpf_map_update_elem(&connect_info_map, &pid_tgid, &info, BPF_ANY);
bpf_map_lookup_elem(&connect_info_map, &pid_tgid);
bpf_map_delete_elem(&connect_info_map, &pid_tgid);
```

BCC perf output：

```c
BPF_PERF_OUTPUT(events);
events.perf_submit(ctx, &event, sizeof(event));
```

libbpf ringbuf：

```c
struct netdisk_event *event;

event = bpf_ringbuf_reserve(&events, sizeof(*event), 0);
if (!event)
    return 0;

event->kind = NETDISK_EVENT_NETWORK;
/* fill event */

bpf_ringbuf_submit(event, 0);
```

BCC 头文件：

```c
#include <bcc/proto.h>
```

libbpf/CO-RE 不使用 BCC 头文件，应改为：

```c
#include "vmlinux.h"
#include <bpf/bpf_helpers.h>
#include <bpf/bpf_tracing.h>
#include <bpf/bpf_core_read.h>
```

## 8. NetworkMonitor 迁移设计

先实现三个动作：

| 动作 | 推荐 attach 点 |
|------|----------------|
| 主动连接 | `kprobe/tcp_v4_connect` + `kretprobe/tcp_v4_connect` |
| 被动接受 | `kretprobe/inet_csk_accept` |
| 关闭连接 | `kprobe/tcp_close` |

### 8.1 获取 socket 地址

原 BCC 代码中直接读：

```c
sk->__sk_common.skc_rcv_saddr
sk->__sk_common.skc_daddr
sk->__sk_common.skc_num
sk->__sk_common.skc_dport
```

CO-RE 推荐用 `BPF_CORE_READ`：

```c
static __always_inline void read_sock_info(
    struct sock *sk,
    __u32 *saddr,
    __u32 *daddr,
    __u16 *sport,
    __u16 *dport
) {
    __u16 dport_be;

    *saddr = BPF_CORE_READ(sk, __sk_common.skc_rcv_saddr);
    *daddr = BPF_CORE_READ(sk, __sk_common.skc_daddr);
    *sport = BPF_CORE_READ(sk, __sk_common.skc_num);
    dport_be = BPF_CORE_READ(sk, __sk_common.skc_dport);
    *dport = bpf_ntohs(dport_be);
}
```

如果使用 `bpf_ntohs`，可能需要：

```c
#include <bpf/bpf_endian.h>
```

### 8.2 connect 入口

```c
SEC("kprobe/tcp_v4_connect")
int BPF_KPROBE(handle_tcp_v4_connect, struct sock *sk)
{
    __u64 pid_tgid = bpf_get_current_pid_tgid();
    struct connect_info info = {};

    info.ts = bpf_ktime_get_ns();
    info.sk = sk;

    bpf_map_update_elem(&connect_info_map, &pid_tgid, &info, BPF_ANY);
    return 0;
}
```

### 8.3 connect 返回

```c
SEC("kretprobe/tcp_v4_connect")
int BPF_KRETPROBE(handle_tcp_v4_connect_ret, int ret)
{
    __u64 pid_tgid = bpf_get_current_pid_tgid();
    struct connect_info *info;
    struct netdisk_event *event;

    info = bpf_map_lookup_elem(&connect_info_map, &pid_tgid);
    if (!info)
        return 0;

    if (ret != 0 && ret != -115) {
        bpf_map_delete_elem(&connect_info_map, &pid_tgid);
        return 0;
    }

    event = bpf_ringbuf_reserve(&events, sizeof(*event), 0);
    if (!event) {
        bpf_map_delete_elem(&connect_info_map, &pid_tgid);
        return 0;
    }

    event->kind = NETDISK_EVENT_NETWORK;
    event->network.pid = pid_tgid >> 32;
    event->network.tid = (__u32)pid_tgid;
    event->network.action = NETDISK_NET_CONNECT;
    event->network.timestamp_ns = bpf_ktime_get_ns();
    bpf_get_current_comm(&event->network.comm, sizeof(event->network.comm));
    read_sock_info(
        info->sk,
        &event->network.saddr,
        &event->network.daddr,
        &event->network.sport,
        &event->network.dport
    );

    bpf_ringbuf_submit(event, 0);
    bpf_map_delete_elem(&connect_info_map, &pid_tgid);
    return 0;
}
```

注意：`-115` 是 Linux 上常见的 `-EINPROGRESS`。如果希望更严谨，可以包含对应常量，但在 BPF C 中为了兼容性常直接写数值。

## 9. send/recv 字节统计的正确做法

原 BCC 版本在 `tcp_sendmsg` / `tcp_recvmsg` 入口统计参数长度：

```c
event.bytes = size;
event.bytes = len;
```

这不是实际成功发送/接收的字节数。libbpf 迁移时建议改成：

1. 入口 kprobe 保存 `struct sock *sk` 和请求大小。
2. 返回 kretprobe 读取 `ret`。
3. 只有 `ret > 0` 时把 `ret` 作为实际字节数。

临时 map 可以定义：

```c
struct io_info {
    struct sock *sk;
    __u64 requested;
};

struct {
    __uint(type, BPF_MAP_TYPE_HASH);
    __uint(max_entries, 10240);
    __type(key, __u64);
    __type(value, struct io_info);
} send_info_map SEC(".maps");
```

入口：

```c
SEC("kprobe/tcp_sendmsg")
int BPF_KPROBE(handle_tcp_sendmsg, struct sock *sk, struct msghdr *msg, size_t size)
{
    __u64 pid_tgid = bpf_get_current_pid_tgid();
    struct io_info info = {};

    info.sk = sk;
    info.requested = size;
    bpf_map_update_elem(&send_info_map, &pid_tgid, &info, BPF_ANY);
    return 0;
}
```

返回：

```c
SEC("kretprobe/tcp_sendmsg")
int BPF_KRETPROBE(handle_tcp_sendmsg_ret, int ret)
{
    __u64 pid_tgid = bpf_get_current_pid_tgid();
    struct io_info *info;

    info = bpf_map_lookup_elem(&send_info_map, &pid_tgid);
    if (!info)
        return 0;

    if (ret > 0) {
        /* 输出 NETDISK_NET_SEND 事件，bytes = ret */
    }

    bpf_map_delete_elem(&send_info_map, &pid_tgid);
    return 0;
}
```

`tcp_recvmsg` 同理。

## 10. libbpf loader 设计

`netdisk_loader.c` 负责：

1. 打开 skeleton。
2. 加载 BPF 程序。
3. attach 探针。
4. 创建 ring buffer。
5. 循环 poll。
6. 把事件打印为一行 JSON。

基本结构：

```c
#include <stdio.h>
#include <signal.h>
#include <unistd.h>
#include <arpa/inet.h>
#include <bpf/libbpf.h>
#include "include/events.h"
#include "netdisk_bpf.skel.h"

static volatile sig_atomic_t exiting = 0;

static void handle_signal(int sig)
{
    exiting = 1;
}

static int handle_event(void *ctx, void *data, size_t data_sz)
{
    const struct netdisk_event *event = data;

    if (event->kind == NETDISK_EVENT_NETWORK) {
        char saddr[INET_ADDRSTRLEN];
        char daddr[INET_ADDRSTRLEN];

        inet_ntop(AF_INET, &event->network.saddr, saddr, sizeof(saddr));
        inet_ntop(AF_INET, &event->network.daddr, daddr, sizeof(daddr));

        printf(
            "{\"type\":\"network\",\"action\":%u,\"pid\":%u,"
            "\"comm\":\"%s\",\"saddr\":\"%s\",\"daddr\":\"%s\","
            "\"sport\":%u,\"dport\":%u,\"bytes\":%llu}\n",
            event->network.action,
            event->network.pid,
            event->network.comm,
            saddr,
            daddr,
            event->network.sport,
            event->network.dport,
            (unsigned long long)event->network.bytes
        );
        fflush(stdout);
    }

    return 0;
}

int main(int argc, char **argv)
{
    struct netdisk_bpf_bpf *skel;
    struct ring_buffer *rb = NULL;
    int err;

    signal(SIGINT, handle_signal);
    signal(SIGTERM, handle_signal);

    skel = netdisk_bpf_bpf__open_and_load();
    if (!skel) {
        fprintf(stderr, "failed to open and load BPF skeleton\n");
        return 1;
    }

    err = netdisk_bpf_bpf__attach(skel);
    if (err) {
        fprintf(stderr, "failed to attach BPF programs: %d\n", err);
        goto cleanup;
    }

    rb = ring_buffer__new(
        bpf_map__fd(skel->maps.events),
        handle_event,
        NULL,
        NULL
    );
    if (!rb) {
        fprintf(stderr, "failed to create ring buffer\n");
        err = 1;
        goto cleanup;
    }

    while (!exiting) {
        err = ring_buffer__poll(rb, 100);
        if (err == -EINTR)
            break;
        if (err < 0) {
            fprintf(stderr, "ring_buffer__poll failed: %d\n", err);
            break;
        }
    }

cleanup:
    ring_buffer__free(rb);
    netdisk_bpf_bpf__destroy(skel);
    return err < 0 ? -err : err;
}
```

注意 skeleton 类型名和函数名由 BPF 文件名决定。`netdisk_bpf.bpf.c` 生成的 skeleton 通常会出现类似：

```c
struct netdisk_bpf_bpf;
netdisk_bpf_bpf__open_and_load();
netdisk_bpf_bpf__attach();
netdisk_bpf_bpf__destroy();
```

实际名称以生成的 `netdisk_bpf.skel.h` 为准。

## 11. Makefile 设计

示例：

```makefile
CLANG ?= clang
CC ?= gcc
BPFTOOL ?= bpftool

ARCH := $(shell uname -m | sed 's/x86_64/x86/' | sed 's/aarch64/arm64/')

CFLAGS := -g -O2 -Wall
BPF_CFLAGS := -g -O2 -target bpf -D__TARGET_ARCH_$(ARCH)

LIBS := -lbpf -lelf -lz

all: netdisk_loader

vmlinux.h:
	$(BPFTOOL) btf dump file /sys/kernel/btf/vmlinux format c > $@

netdisk_bpf.bpf.o: netdisk_bpf.bpf.c include/events.h vmlinux.h
	$(CLANG) $(BPF_CFLAGS) -I. -Iinclude -c $< -o $@

netdisk_bpf.skel.h: netdisk_bpf.bpf.o
	$(BPFTOOL) gen skeleton $< > $@

netdisk_loader: netdisk_loader.c netdisk_bpf.skel.h include/events.h
	$(CC) $(CFLAGS) -I. -Iinclude -o $@ netdisk_loader.c $(LIBS)

clean:
	rm -f netdisk_loader *.bpf.o *.skel.h

.PHONY: all clean
```

构建：

```bash
cd ebpf_debugger/libbpf
make
```

运行：

```bash
sudo ./netdisk_loader
```

另开终端制造网络事件：

```bash
curl http://127.0.0.1:8080/api/health
```

或启动项目自己的 C server/client。

## 12. Python 适配层设计

现有 `app.py` 期望这些类仍然存在：

```python
NetworkMonitor
SyscallTracer
PerfAnalyzer
UprobeTracer
```

因此不建议改 `app.py` 的接口。改 `network_monitor.py` 即可：

```python
import json
import os
import subprocess
import threading


class NetworkMonitor:
    def __init__(self, collector, pid_filter=None):
        self.collector = collector
        self.pid_filter = pid_filter
        self.proc = None
        self.thread = None
        self.running = False

    def start(self):
        loader = os.path.abspath(
            os.path.join(os.path.dirname(__file__), "..", "libbpf", "netdisk_loader")
        )

        self.proc = subprocess.Popen(
            [loader, "--network"],
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True,
            bufsize=1,
        )

        self.running = True
        self.thread = threading.Thread(target=self._read_loop, daemon=True)
        self.thread.start()

    def poll(self, timeout=100):
        # libbpf loader 自己在后台 poll；这里保留接口兼容 app.py
        return

    def stop(self):
        self.running = False
        if self.proc:
            self.proc.terminate()
            self.proc.wait(timeout=2)
            self.proc = None

    def _read_loop(self):
        for line in self.proc.stdout:
            if not self.running:
                break
            self._handle_json(line)

    def _handle_json(self, line):
        event = json.loads(line)
        if event.get("type") != "network":
            return

        action = event["action"]
        pid = event["pid"]
        comm = event["comm"]
        saddr = event["saddr"]
        daddr = event["daddr"]
        sport = event["sport"]
        dport = event["dport"]
        bytes_count = event.get("bytes", 0)

        if action == 1:
            self.collector.on_connect(pid, comm, saddr, daddr, sport, dport)
        elif action == 2:
            self.collector.on_connect(pid, comm, saddr, daddr, sport, dport)
        elif action == 3:
            self.collector.on_close(pid, saddr, daddr, sport, dport)
        elif action == 4:
            self.collector.on_send(pid, saddr, daddr, sport, dport, bytes_count)
        elif action == 5:
            self.collector.on_recv(pid, saddr, daddr, sport, dport, bytes_count)
```

实际实现时建议再加：

- loader 不存在时给出明确错误。
- stderr 后台读取，避免 pipe 堵塞。
- `stop()` 里处理 `TimeoutExpired`，必要时 kill。
- JSON 解析失败时忽略该行并打印日志。

## 13. 是否一个 loader 管全部模块

有两种设计。

### 方案 A：一个 loader 管全部模块

命令：

```bash
sudo ./netdisk_loader --network --syscall --perf --uprobe --server-path ../../server/server
```

优点：

- 只加载一组 BPF 程序。
- ringbuf 统一。
- Python 只需要一个后台进程。

缺点：

- 早期开发复杂。
- 某个模块 attach 失败时需要更细的错误处理。

### 方案 B：每个模块一个 loader 或一个 loader 多模式

命令：

```bash
sudo ./netdisk_loader --network
sudo ./netdisk_loader --syscall
sudo ./netdisk_loader --perf
```

优点：

- 便于逐步迁移。
- 某个模块失败不会影响其他模块。
- 更接近当前 Python monitor 的结构。

缺点：

- 多个进程。
- 有些事件结构重复。

建议先用方案 B，等四个模块都稳定后再考虑合并。

## 14. syscall tracer 迁移思路

原 BCC 使用：

```c
TRACEPOINT_PROBE(syscalls, sys_enter_read) { ... }
TRACEPOINT_PROBE(syscalls, sys_exit_read) { ... }
```

libbpf 写法：

```c
SEC("tracepoint/syscalls/sys_enter_read")
int handle_enter_read(struct trace_event_raw_sys_enter *ctx)
{
    return trace_syscall_enter("read");
}

SEC("tracepoint/syscalls/sys_exit_read")
int handle_exit_read(struct trace_event_raw_sys_exit *ctx)
{
    return trace_syscall_exit(ctx, "read");
}
```

常见结构：

```c
struct syscall_start {
    __u64 ts;
};

struct {
    __uint(type, BPF_MAP_TYPE_HASH);
    __uint(max_entries, 10240);
    __type(key, __u64);
    __type(value, struct syscall_start);
} syscall_start_map SEC(".maps");
```

注意：

- `pid_tgid` 作为 key 对 syscall entry/exit 一般可行。
- 如果只在 exit 阶段做 PID 过滤，非目标进程的 entry 也会进入 map，建议 entry 阶段也过滤。
- 文件名追踪比 syscall 名称追踪复杂，先不要做。后续可以通过 `openat` entry 的参数读取用户态字符串。

## 15. perf analyzer 迁移思路

上下文切换：

```c
SEC("tracepoint/sched/sched_switch")
int handle_sched_switch(struct trace_event_raw_sched_switch *ctx)
{
    /* 输出 prev_pid / next_pid / comm */
    return 0;
}
```

CPU 采样有两种做法：

1. 用户态 `perf_event_open` + `bpf_program__attach_perf_event`。
2. libbpf skeleton auto attach 不一定能直接覆盖这个场景。

建议先迁移 `sched_switch`，CPU 采样后做。原 BCC 版本里的 CPU 使用率本来就不是严格百分比，迁移时可以重新设计为“采样数/时间窗口/CPU 数”的估算。

## 16. uprobe tracer 迁移思路

uprobe 是最后迁移的模块。原因：

- 依赖 `server/server` 已经编译存在。
- 依赖函数符号没有被优化掉。
- `handle_client`、`send_directory_tree`、`sha256_string` 之间存在嵌套调用。

原 BCC 版本用单个 `pid_tgid -> start_ts`，嵌套调用会覆盖外层计时。libbpf 迁移时建议改成：

```c
struct func_key {
    __u64 pid_tgid;
    __u32 func_id;
};
```

map：

```c
struct {
    __uint(type, BPF_MAP_TYPE_HASH);
    __uint(max_entries, 10240);
    __type(key, struct func_key);
    __type(value, __u64);
} func_start_map SEC(".maps");
```

这样 `handle_client` 和 `sha256_string` 不会互相覆盖。

用户态 attach 方式通常不是 skeleton auto attach，而是在 loader 里显式调用：

```c
bpf_program__attach_uprobe(
    skel->progs.handle_handle_client_entry,
    false,
    -1,
    server_path,
    0
);
```

更常见流程是：

1. 用 `elf` 或 `nm` 查函数 offset。
2. 用 `bpf_program__attach_uprobe()` attach 到该 offset。
3. return probe 使用 `retprobe=true`。

验证符号：

```bash
nm -an server/server | grep ' handle_client\| send_directory_tree\| sha256_string'
```

如果没有符号，尝试：

```bash
make debug
```

或者降低优化等级，避免函数被内联。

## 17. 启动脚本改造

当前 `start_debugger.sh` 检查 BCC：

```bash
python3 -c "from bcc import BPF"
```

迁移后应改为检查：

```bash
command -v clang
command -v bpftool
pkg-config --exists libbpf
test -e /sys/kernel/btf/vmlinux
test -x ebpf_debugger/libbpf/netdisk_loader
```

可选：启动前自动构建 libbpf helper：

```bash
(cd "$SCRIPT_DIR/libbpf" && make)
```

注意 `SCRIPT_DIR` 当前是 `ebpf_debugger`，所以 libbpf 路径应是：

```bash
cd "$SCRIPT_DIR/libbpf"
```

## 18. README 需要同步的内容

把技术栈从：

```text
Python + BCC + WebSocket
```

改成：

```text
Python + libbpf/CO-RE + WebSocket
```

依赖从：

```bash
sudo apt install bpfcc-tools linux-headers-$(uname -r) python3-bpfcc
```

改成：

```bash
sudo apt install clang llvm bpftool libbpf-dev linux-headers-$(uname -r)
```

并说明：

- 首次运行前需要构建 `ebpf_debugger/libbpf/netdisk_loader`。
- eBPF 加载仍需要 root/capability。
- uprobe 需要先编译 `server/server`。

## 19. 验证清单

### 19.1 构建验证

```bash
cd ebpf_debugger/libbpf
make clean
make
```

期望产物：

```text
netdisk_bpf.bpf.o
netdisk_bpf.skel.h
netdisk_loader
```

### 19.2 BPF 加载验证

```bash
sudo ./netdisk_loader --network
```

另开终端：

```bash
curl http://127.0.0.1:8080/api/health
```

期望 loader 输出 JSON 行。

### 19.3 Python 适配验证

```bash
sudo python3 ebpf_debugger/app.py
```

访问：

```text
http://localhost:8090
```

检查：

```bash
curl http://localhost:8090/status
curl http://localhost:8090/api/network
```

### 19.4 bpftool 验证

```bash
sudo bpftool prog list
sudo bpftool map list
```

如果 loader 运行中，应能看到相关 BPF program/map。

## 20. 常见问题

### 20.1 `failed to open and load BPF skeleton`

常见原因：

- 没有 root 权限。
- `RLIMIT_MEMLOCK` 限制。
- 内核不支持某些 helper。
- BTF 不存在或 CO-RE relocation 失败。

可以在 loader 中加：

```c
libbpf_set_strict_mode(LIBBPF_STRICT_ALL);
libbpf_set_print(libbpf_print_fn);
```

打印 libbpf 详细日志。

### 20.2 verifier 报 invalid memory access

常见原因：

- 直接解引用内核指针。
- 没有用 `BPF_CORE_READ`。
- ringbuf reserve 后某些字段没有初始化。
- 分支路径里 submit/discard 不完整。

处理原则：

- 所有事件结构先清零或逐字段完整赋值。
- 内核结构字段用 `BPF_CORE_READ`。
- ringbuf reserve 失败立即返回。
- reserve 成功后必须 submit 或 discard。

### 20.3 没有网络事件

检查：

```bash
sudo cat /sys/kernel/debug/tracing/available_filter_functions | grep tcp_v4_connect
sudo cat /sys/kernel/debug/tracing/available_filter_functions | grep inet_csk_accept
```

如果函数名不存在，说明当前内核符号不同，需要换 attach 点或使用 tracepoint。

### 20.4 IP 地址显示反了

内核里 IPv4 地址字段通常是网络字节序。用户态 `inet_ntop` 时不要随意 `htonl`/`ntohl`，先实测。如果显示反了，再统一调整。

### 20.5 Python 面板没有数据，但 loader 有输出

检查：

- Python 是否启动的是正确路径下的 `netdisk_loader`。
- loader 输出是否每行一个完整 JSON。
- Python 是否读取了 stdout。
- stderr pipe 是否堵塞。
- JSON 字段名是否和适配层一致。

## 21. 建议的提交拆分

建议分多次 commit：

1. `docs: add bcc to libbpf migration guide`
2. `build: add libbpf build scaffold`
3. `ebpf: add network bpf program and loader`
4. `debugger: adapt network monitor to libbpf loader`
5. `ebpf: add syscall tracepoints`
6. `ebpf: add sched/perf monitoring`
7. `ebpf: add uprobe tracing`
8. `docs: update readme for libbpf debugger`

这样任何一步出问题都可以单独回退。

## 22. 最小可行目标

第一阶段不要追求功能全量等价。最小可行目标是：

1. `make` 能构建 libbpf loader。
2. `sudo ./netdisk_loader --network` 能输出 TCP connect/accept/close JSON。
3. `NetworkMonitor` 能消费 JSON 并更新原有 `NetworkCollector`。
4. Web 面板能看到连接数变化。

完成这四点后，再继续迁移 send/recv、syscall、perf、uprobe。

